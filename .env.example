# Local development environment variables
# Copy to .env and fill with your local MySQL credentials

# App
PORT=5175

# Database (custom names used by this project)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=your_database

# Note: On Railway, if you add the MySQL plugin, it provides these automatically:
# MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE
# Our server supports both DB_* and MYSQL* names.
